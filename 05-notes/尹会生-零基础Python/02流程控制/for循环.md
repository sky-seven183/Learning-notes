# for循环

> **用途**：循环编历，避免代码重复
---

## 核心一句话
> for循环的使用
> range()
> in
> 嵌套循环

## 知识点

1 for循环，循环遍历，区别while的条件判断，用途不同，主要用于可迭代数据类型的数据处理
-
2 range()  左闭右开，可以加步长（间隔），range(start,stop,step)）
-
3 嵌套，先看内层循环，再看外层循环
-